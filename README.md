# KEYS
  - --help(-h)                        - messages with program options

  - --version(-v)                     - version

  - --name(-n)                         - file name

  - --list(-l)=[...,...] - list of arguments
